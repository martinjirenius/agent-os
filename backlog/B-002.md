---
id: B-002
title: git notes helpers — session profile and hindsight annotation
status: todo
serves: D-01
opened: 2026-08-02
---

Two uses (docs/02-git-model.md): /wrap flushes the session dive profile to a note on the
final commit; a later session annotates an *earlier* commit when it learns that approach
was wrong, so the correction lives where the mistake lives.

First move: `tools/git_notes.py add-profile` / `annotate`, plus the push refspec config
(`refs/notes/*`) since silent loss on push is the known gotcha.
